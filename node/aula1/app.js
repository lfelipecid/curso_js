const mod1 = require('./mod1') // Importação
const { falaNome } = require('./mod1') // Importação
const { Pessoa } = require('./mod1') // Importação

console.log(falaNome())


const p1 = new Pessoa('Felipe', 'Cid')
console.log(p1)

